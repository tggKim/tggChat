<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tgg.chat.domain.chat.repository.ChatRoomMapper">
    <select id="findActiveChatRoomsByUserId">
        SELECT
            cr.chat_room_id                 AS roomId
        ,   cr.chat_room_type               AS roomType
        ,   cr.room_name                    AS baseRoomName
        ,   cru.custom_room_name            AS customRoomName
        ,   cru.chat_room_user_role         AS myRole
        ,   cru.joined_at                   AS joinedAt
        ,   cru.unread_start_message_id     AS unreadStartMessageId
        FROM chat_room_user cru
        INNER JOIN chat_room cr
            ON cru.chat_room_id = cr.chat_room_id
        WHERE cru.user_id = #{userId}
        AND cru.chat_room_user_status = 'ACTIVE'
    </select>

    <select id="findMemberCountsByChatRoomIds">
        SELECT
            cru.chat_room_id    AS roomId
        ,   COUNT(*)            AS memberCount
        FROM chat_room_user cru
        INNER JOIN user u
            ON cru.user_id = u.user_id
        INNER JOIN chat_room cr
            ON cru.chat_room_id = cr.chat_room_id
        WHERE cru.chat_room_id IN
        <foreach collection="roomIds" item="roomId" open="(" separator="," close=")">
            #{roomId}
        </foreach>
        AND (
            cru.chat_room_user_status = 'ACTIVE'
            OR
            cr.chat_room_type = 'DIRECT'
        )
        AND u.deleted = false
        GROUP BY cru.chat_room_id
    </select>

    <select id="findPreviewUsersByUserIdAndChatRoomIds">
        SELECT
            ranked.chat_room_id        AS roomId
        ,   ranked.user_id             AS userId
        ,   ranked.username            AS username
        ,   ranked.profile_image_key   AS profileImageKey
        FROM (
            SELECT
                cru.chat_room_id
            ,   cru.user_id
            ,   u.username
            ,   u.profile_image_key
            ,   ROW_NUMBER() OVER(
                PARTITION BY cru.chat_room_id
                ORDER BY u.username
            ) AS preview_order
            FROM chat_room_user cru
            INNER JOIN chat_room cr
                ON cru.chat_room_id = cr.chat_room_id
            INNER JOIN user u
                ON cru.user_id = u.user_id
            WHERE cru.chat_room_id IN
            <foreach collection="roomIds" item="roomId" open="(" separator="," close=")">
                #{roomId}
            </foreach>
            AND (
                cr.chat_room_type = 'DIRECT'
                OR
                cru.chat_room_user_status = 'ACTIVE'
            )
            AND cru.user_id &lt;&gt; #{userId}
            AND u.deleted = false
        ) ranked
        WHERE ranked.preview_order &lt;= 4
        ORDER BY ranked.chat_room_id, ranked.preview_order
    </select>

    <select id="findLatestVisibleMessagesByUserIdAndChatRoomIds">
        SELECT
            ranked.chat_room_id     AS roomId
        ,   ranked.content          AS lastMessagePreview
        ,   ranked.chat_message_id  AS messageId
        ,   ranked.created_at       AS createdAt
        FROM (
            SELECT
                cm.chat_room_id
            ,   cm.chat_message_id
            ,   cm.content
            ,   cm.created_at
            ,   ROW_NUMBER() OVER (
                PARTITION BY cm.chat_room_id
                ORDER BY cm.chat_message_id DESC
            ) AS message_order
            FROM chat_message cm
            INNER JOIN chat_room_user cru
                ON cru.chat_room_id = cm.chat_room_id
                AND cru.user_id = #{userId}
            WHERE cm.chat_room_id IN
            <foreach collection="roomIds" item="roomId" open="(" separator="," close=")">
                #{roomId}
            </foreach>
            AND cm.chat_message_id &gt;= cru.visible_start_message_id
        ) ranked
        WHERE ranked.message_order = 1
        ORDER BY ranked.chat_room_id
    </select>

    <select id="findUnreadMessageCountsByUserIdAndChatRoomIds">
        SELECT
            cm.chat_room_id     AS roomId
        ,   COUNT(*)            AS unreadCount
        FROM chat_message cm
        INNER JOIN chat_room_user cru
            ON cru.chat_room_id = cm.chat_room_id
            AND cru.user_id = #{userId}
        WHERE cm.chat_room_id IN
        <foreach collection="roomIds" item="roomId" open="(" separator="," close=")">
            #{roomId}
        </foreach>
        AND cm.chat_message_id &gt;= cru.unread_start_message_id
        GROUP BY cm.chat_room_id
        ORDER BY cm.chat_room_id
    </select>
</mapper>