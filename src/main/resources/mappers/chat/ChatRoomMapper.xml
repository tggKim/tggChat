<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tgg.chat.domain.chat.room.repository.ChatRoomMapper">

    <select id="existsDirectChatRoom" parameterType="long" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM chat_room
            WHERE chat_room_type = 'DIRECT'
            AND direct_user1_id = #{userId1}
            AND direct_user2_id = #{userId2}
        )
    </select>

</mapper>